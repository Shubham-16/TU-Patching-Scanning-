request_with_ip: {
      "engineId": "{{scan_eng_id}}",
      "hosts": "{{ valid_ip_list }}",
     "name": "{{scan_name}}",
     "templateId": "{{ scan_template_id }}"
     }

request_full_scan: {
      "engineId": "{{scan_eng_id}}",
     "name": "{{scan_name}}",
     "templateId": "{{ scan_template_id }}"
     }

date: "{{ lookup('pipe', 'date +%Y-%m-%d')}}"
time: "{{ lookup('pipe', 'date +%H:%M:%S')}}"
#valid_ip_list: []